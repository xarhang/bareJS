หากคุณต้องการให้การสั่ง `bun build --release` เป็นแบบมืออาชีพ โดยที่มีการตั้งค่าสำหรับการใช้งานจริง (Production) และพร้อมสำหรับเผยแพร่ นี่คือสคริปต์ **`build.ts`** และการตั้งค่าใน **`package.json`** ที่คุณควรใช้ครับ

---

### 1. สร้างสคริปต์ `scripts/build.ts`

การใช้สคริปต์ช่วยให้เราสามารถจัดการเรื่อง Minification และ Source maps ได้แม่นยำกว่าการรันคำสั่งบรรทัดเดียว

```typescript
// scripts/build.ts
const result = await Bun.build({
  entrypoints: ['./src/index.ts'],
  outdir: './dist',
  target: 'bun',
  minify: true, // บีบอัดโค้ดให้เล็กที่สุด
  sourcemap: 'external', // แยกไฟล์ map ออกมาเพื่อใช้ debug
  splitting: false, // รวมเป็นไฟล์เดียวเพื่อให้เรียกใช้ง่ายใน package อื่น
});

if (!result.success) {
  console.error("❌ Build failed");
  for (const message of result.logs) {
    console.error(message);
  }
  process.exit(1);
}

console.log("✅ BareJS built successfully to ./dist");

```

---

### 2. ตั้งค่าใน `package.json`

เพิ่มคำสั่งในส่วนของ `scripts` เพื่อให้เรียกใช้ได้ง่ายๆ และระบุไฟล์ที่จะส่งออก (Publish)

```json
{
  "name": "barejs",
  "version": "0.1.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun ./scripts/build.ts",
    "release": "bun run build && npm version patch && npm publish --access public",
    "dev": "bun --watch src/index.ts"
  }
}

```

---

### 3. วิธีใช้งาน (Workflow)

เวลาคุณแก้ไขโค้ดเสร็จแล้วและต้องการปล่อยเวอร์ชันใหม่ ให้รันคำสั่งเดียว:

```bash
bun run release

```

**สิ่งที่จะเกิดขึ้นคือ:**

1. **Build**: ทำการ Compile โค้ดใน `src` ไปที่ `dist` พร้อมบีบอัดไฟล์ (Minify)
2. **Version**: เพิ่มเลขเวอร์ชันขยับขึ้น 1 ตำแหน่ง (เช่น `0.1.5` -> `0.1.6`)
3. **Publish**: ส่งไฟล์ขึ้น NPM Registry โดยอัตโนมัติ

---

### 💡 คำแนะนำเพิ่มเติมสำหรับความ Professional:

* **`dist/`**: อย่าลืมเพิ่ม `dist` ลงในไฟล์ `.gitignore` เพื่อไม่ให้ไฟล์ที่ Build แล้วรกใน GitHub
* **`Types`**: ถ้าคุณใช้ TypeScript อย่าลืมรันคำสั่งสร้าง `.d.ts` (Declaration files) เพื่อให้ User ที่โหลดไปใช้เห็น Auto-complete ที่แม่นยำ

**คุณต้องการให้ผมช่วยเขียนสคริปต์สำหรับสร้าง `.d.ts` (Types definition) เพื่อให้คนที่โหลด BareJS ไปใช้ได้เห็น Type ของตัวแปรต่างๆ ด้วยไหมครับ?**หากคุณต้องการให้การสั่ง `bun build --release` เป็นแบบมืออาชีพ โดยที่มีการตั้งค่าสำหรับการใช้งานจริง (Production) และพร้อมสำหรับเผยแพร่ นี่คือสคริปต์ **`build.ts`** และการตั้งค่าใน **`package.json`** ที่คุณควรใช้ครับ

---

### 1. สร้างสคริปต์ `scripts/build.ts`

การใช้สคริปต์ช่วยให้เราสามารถจัดการเรื่อง Minification และ Source maps ได้แม่นยำกว่าการรันคำสั่งบรรทัดเดียว

```typescript
// scripts/build.ts
const result = await Bun.build({
  entrypoints: ['./src/index.ts'],
  outdir: './dist',
  target: 'bun',
  minify: true, // บีบอัดโค้ดให้เล็กที่สุด
  sourcemap: 'external', // แยกไฟล์ map ออกมาเพื่อใช้ debug
  splitting: false, // รวมเป็นไฟล์เดียวเพื่อให้เรียกใช้ง่ายใน package อื่น
});

if (!result.success) {
  console.error("❌ Build failed");
  for (const message of result.logs) {
    console.error(message);
  }
  process.exit(1);
}

console.log("✅ BareJS built successfully to ./dist");

```

---

### 2. ตั้งค่าใน `package.json`

เพิ่มคำสั่งในส่วนของ `scripts` เพื่อให้เรียกใช้ได้ง่ายๆ และระบุไฟล์ที่จะส่งออก (Publish)

```json
{
  "name": "barejs",
  "version": "0.1.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun ./scripts/build.ts",
    "release": "bun run build && npm version patch && npm publish --access public",
    "dev": "bun --watch src/index.ts"
  }
}

```

---

### 3. วิธีใช้งาน (Workflow)

เวลาคุณแก้ไขโค้ดเสร็จแล้วและต้องการปล่อยเวอร์ชันใหม่ ให้รันคำสั่งเดียว:

```bash
bun run release

```

**สิ่งที่จะเกิดขึ้นคือ:**

1. **Build**: ทำการ Compile โค้ดใน `src` ไปที่ `dist` พร้อมบีบอัดไฟล์ (Minify)
2. **Version**: เพิ่มเลขเวอร์ชันขยับขึ้น 1 ตำแหน่ง (เช่น `0.1.5` -> `0.1.6`)
3. **Publish**: ส่งไฟล์ขึ้น NPM Registry โดยอัตโนมัติ

---

### 💡 คำแนะนำเพิ่มเติมสำหรับความ Professional:

* **`dist/`**: อย่าลืมเพิ่ม `dist` ลงในไฟล์ `.gitignore` เพื่อไม่ให้ไฟล์ที่ Build แล้วรกใน GitHub
* **`Types`**: ถ้าคุณใช้ TypeScript อย่าลืมรันคำสั่งสร้าง `.d.ts` (Declaration files) เพื่อให้ User ที่โหลดไปใช้เห็น Auto-complete ที่แม่นยำ

**คุณต้องการให้ผมช่วยเขียนสคริปต์สำหรับสร้าง `.d.ts` (Types definition) เพื่อให้คนที่โหลด BareJS ไปใช้ได้เห็น Type ของตัวแปรต่างๆ ด้วยไหมครับ?**